=begin
Define a function that removes duplicates from an array of numbers and returns it as a result.

The order of the sequence has to stay the same.
=end


def distinct(seq)
    return seq.uniq
end

#Uniq Returns Array by Removing Duplicate Values in the Input.